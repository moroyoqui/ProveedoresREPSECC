# =============================================================================
# ProveedoresREPSECC — environment variables
# Copia este archivo como .env en la raíz del repo y rellena los valores.
# =============================================================================

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
APP_ENV=local                      # local | staging | prod
APP_SECRET=change-me-to-32-bytes-random
APP_BASE_URL=https://localhost
UPLOAD_ROOT=/var/repse/uploads     # ruta absoluta dentro del contenedor app
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Database (MySQL 8.0)
# -----------------------------------------------------------------------------
DB_HOST=mysql
DB_PORT=3306
DB_NAME=repse
DB_USER=repse
DB_PASS=change-me

# Para el contenedor mysql (init):
MYSQL_ROOT_PASSWORD=change-me-too
MYSQL_DATABASE=repse
MYSQL_USER=repse
MYSQL_PASSWORD=change-me

# -----------------------------------------------------------------------------
# OAuth / OIDC
# -----------------------------------------------------------------------------
OIDC_GOOGLE_CLIENT_ID=
OIDC_GOOGLE_CLIENT_SECRET=
OIDC_MICROSOFT_CLIENT_ID=
OIDC_MICROSOFT_CLIENT_SECRET=
OIDC_MICROSOFT_TENANT=common       # 'common' acepta cualquier Entra; 'consumers' solo personal

# -----------------------------------------------------------------------------
# OCR (Tesseract)
# -----------------------------------------------------------------------------
TESSERACT_LANG=spa+eng

# -----------------------------------------------------------------------------
# Observability (opcional)
# -----------------------------------------------------------------------------
SENTRY_DSN=
PROMETHEUS_ENABLED=true

# -----------------------------------------------------------------------------
# Caddy
# -----------------------------------------------------------------------------
CADDY_DOMAIN=localhost             # usar 'tls internal' en local; dominio real en prod
