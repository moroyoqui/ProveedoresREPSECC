"""Compliance alerts & reminders (spec 002).

Daily sweep over required documents per tenant that emits dual-channel
notifications (email + in-app) for documents that are expiring soon or
already expired, with per-org configuration, per-supplier recipient
overrides, and per-document silencing.
"""
